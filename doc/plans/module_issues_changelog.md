# module_issues.md — change log

History of changes to [`module_issues.md`](module_issues.md), newest first.

**Convention — please keep it.** One entry per paragraph, separated by a blank line. Do **not**
concatenate entries onto a single line: this file previously lived as one 15,000-character line
inside `module_issues.md`, which made `git diff` unreadable (38.8 KB of output for three added
lines), broke automated reviewers that read the file, and hid its own content. Add new entries at
the top, under a `## YYYY-MM-DD` heading.

---

## 2026-08-21

**INFRA-038** filed — `connect_to_iEH` and `ssh_to_iEH` are parsed **four incompatible ways across
17 comparisons at 16 distinct lines** (12 connect, 5 ssh): case-insensitive in `setup_library`,
case-sensitive positive `== "True"` in `preprocessing_runoff`/`linear_regression`, case-sensitive
negative `== "False"` in `forecast_library`, and a tri-state parser
(`check_if_ssh_tunnel_is_required`) returning `None` for anything unrecognised. A value like `TRUE`
satisfies neither the positive nor the negative test and lands in an unhandled middle state while
the one validation gate waves it through. Latent on both kghm env files checked; a third per-user
file in the same shared config directory uses `TRUE`. Census corrected after an out-of-loop review
caught the first draft counting an ssh comparison as a connect one and missing the tri-state
parser entirely.

**This change log was extracted** from the trailing paragraph of `module_issues.md`, verbatim and
byte-for-byte (round-trip verified before the split), and reformatted one entry per paragraph.
No wording was altered.


---

## 2026-08-20

Last updated: 2026-08-20 — **INFRA-037 raised Medium → High and confirmed in the field.** A kghm deployment reporting "machine_learning produces no forecasts" was narrowed by eliminating four rival explanations with evidence: the operator's `.env` vs a working one shares 178/182 keys with 168 identical values and **every ML-critical key matching** (the 10 differences are machine paths, secrets, service-container URLs the `apps/` modules never read, and two deliberate threshold choices); the model tree is shared; the API stack was excluded by the owner; and `get_hydroposts_for_pentadal_and_decadal_forecasts()` against the shared station config returns **54 selected stations, 62 flagged available per model** — so `rivers_to_predict` is not empty. What remains is **INFRA-037 + ML-016 together**: `daily` aborts at the expected `preprocessing_runoff` failure before ML runs, and the standalone ML target then crashes on an empty `SAPPHIRE_PREDICTION_MODE` — every route to executing ML is blocked, which fully explains the report. **They should be scheduled as a pair.** Method note worth keeping: the decisive checks were an env-file *key-by-key* comparison and running the station-selection helper directly — not reading code. Earlier same day — Out-of-loop review of the ML runbook found **three Critical defects in it**, all fixed before delivery, and all worth knowing because they are traps about this repo's logging rather than about the runbook: (1) the ML module's records **do** reach `apps/logs/run_locally_*.log` — `make_forecast.py:96` leaves the console handler unattached, but the named logger propagates to a root `StreamHandler`, so an empty ML section **is** evidence ML never started (an earlier draft claimed the opposite; verified against `run_locally_20260210_125845.log`, which carries `Rivers to predict`, `PATH_TO_SCALER` and the write counts); (2) ML reads meteo **API-first** via `read_meteo_data_combined` (`utils_ml_forecast.py:91-118`), so the `*_control_member.csv` mtimes prove nothing unless `SAPPHIRE_API_ENABLED=false`; (3) the API write has a **fourth** silent-skip site — `No ML forecast records to write to API`, `logger.info` and therefore invisible — which is exactly the one that fires when the station intersection is empty. The review also surfaced the **highest-discrimination check in the whole problem**, a bare `print()` at `utils_ml_forecast.py:810` (`SAPPHIRE API: Successfully wrote N ML forecast records`) that `run_in_venv`'s `tee` captures — expect 3 per mode. Two further corrections: the API stores `TiDE`/`TSMixer`, not `TIDE`/`TSMIXER` (`utils_ml_forecast.py:540`), so a naive `model_type=TIDE` query reads as a false failure; and on `daily` a `Skipping machine_learning for PENTAD` line is **healthy**, not a symptom. **ML-021** filed (the ML analogue of PP-051: `make_forecast.py` exits 0 having written no forecasts to the API; **four** silent-skip returns at `utils_ml_forecast.py:737`/`:743`/`:755`/`:816` reach a caller that ignores the return value. **`scr/utils_ml_forecast.py:38` caps the root logger itself**, independently of `setup_library`, so INFRA-029's fix would NOT make the two INFO-level skip messages visible — recorded in the draft as an implementer trap). **INFRA-037** filed while debugging "machine_learning produces no forecasts" on a remote deployment: an expected `preprocessing_runoff` failure aborts the whole `daily` run. `--continue-on-error` was verified to **work** (the `|| { [ "$CONTINUE_ON_ERROR" = false ] && return 1; }` idiom does not trip `set -e`), so the defect is not a broken flag but a mix of (A) seven undifferentiated `sys.exit(1)` sites in `preprocessing_runoff.py` and (B) a failure message that never names the escape hatch. Two leads investigated in the same pass, one confirmed and one **refuted**: the `ML_MODE=DECAD` default does skip ML for PENTAD (`:194`, `should_skip_ml_for_mode` at `:496-501`, `:2154-2158`) — but **not** on the `daily` target, which runs both horizons explicitly (`run_daily_pipeline`, `:1552`); and `resolve_org` does **not** share the codebase's "unset organisation ⇒ demo" assumption — it falls back to empty and runs every module (`resolve_org`, `:562-581`), so a misspelled or absent organisation is harmless in `run_locally.sh` (it is *not* harmless in `preprocessing_runoff.py:130`). Also established: the PREPG-010 transport-retry fix **merged to `maxat_sapphire_2` at 2026-08-20 13:06 CEST** (PR #459, merge commit `e1b9ccd7`; four `_call_with_transport_retry` sites present on trunk), so it is younger than any deployed image tag and no deployment can have it yet — check rebuild time, not image tag. **Method note:** an earlier pass in this session concluded the opposite from a *stale local* `maxat_sapphire_2` ref and from sampling `9f9ad0b5`, which is PREPG-015 (a different, still-unmerged issue that shared the branch). `git fetch origin` and test against `origin/<branch>` before any ancestry claim about trunk. New operator-facing document: [`doc/prod/ml_no_forecasts_debug_runbook.md`](../prod/ml_no_forecasts_debug_runbook.md), rewritten after the review above. Previous entry: 2026-08-18 — Findings from the kyg (kghm) end-to-end local review, folded in: **INFRA-029** filed (`setup_library`'s import-time root-logger config caps 13 entry points at WARNING — proven by direct execution, and the reason PP-045's CLI emits nothing), **INFRA-030** (skipped modules leave no summary line), **ML-019** (`recalculate_nan_forecasts` claims replacement unconditionally), **ML-020** (an all-null ML issue date is unreachable by either maintenance path — investigation). Updated with second/third reproductions or corrections: **PP-040** (the "no env enables ARIMA" survey missed a second config repo), **PREPQ-014** (kyg reproduction at the same cardinality plus owner input that virtual stations legitimately have no norms), **INFRA-026** (kyg snow reproduction with a live backfill boundary), **PP-045** (§D: its own CLI's output is suppressed; runbook corrected). Previous entry: 2026-08-18 (INFRA-022 decision) — Owner chose **option (d)** for the long-term schedule authority: the mode-activity decision gets exactly one definition. Planned in `doc/plans/working/lt_schedule_authority_extraction_plan.md`, now at **rev 4** after five review passes. **Placement, as settled in rev 4**: `apps/long_term_forecasting/lt_schedule_rules.py` — *not* `iEasyHydroForecast`, because validation consumes INFRA-028's manifest rather than the predicates (it already gets horizon values from `long_term_horizon_resolver`). That deleted a whole phase — the `nearest_scheduled_issue_date` move, which had been vestigial since rev 3 put the call back in the caller — plus the wheel-include-list, namespace-mapping, shim and Docker-packaging work that existed only to cross the package boundary. The extraction is **independent cleanup, not a prerequisite** of INFRA-022; its real second consumer is LTF-007. Its placement remains conditional on INFRA-028's open missing-manifest decision, which now gates P1 in the plan's phase graph. Two issues filed out of these reviews: **INFRA-028** (long-term runs emit no record of what they were supposed to produce — the plan's Phase 4 was a placeholder, and a *re-derived* schedule cannot see manual overrides, execution outcome, or a late forecast's snapped-back date; a prerequisite of INFRA-022) and **LTF-007** (the scheduler admits modes at 10 days from issue day while execution refuses at more than 5, so an admitted mode can produce nothing — conditional on both scheduler gates passing and every model's real offset exceeding 5; **semantics: that is a genuine detected execution failure, not a false FAIL**). A **third** live schedule authority was found in the last pass: the local runner's fallback (`run_locally.sh:292`, `is_lt_issue_window`) reimplements the wrap distance and its own five-day window, and is used precisely when the schedule query fails. The review also corrected the plan itself: a mode-level predicate cannot preserve the non-operational short-circuit (which touches no config at all) or the lazy per-model `break`, so the extracted API is a set of caller-driven predicates rather than one function owning the loop; `ForecastConfig` is not merely a loader but **rewrites every model's `general_config.json`** during load; the new module must be added to `iEasyHydroForecast/pyproject.toml`'s explicit wheel include list; `day_distance` must stay importable from `lt_schedule_query`; and the "single-source the tolerance" claim holds only between scheduler and validator — `bin/utils/migration_py/long_forecast.py` keeps its own `_ALWAYS_SKIP_MODES`. **Superseded entries follow — read them as history, not as current guidance.** Where an earlier entry describes the extraction as landing in `iEasyHydroForecast`, requiring a wheel-include-list update, or sharing a tolerance with the validator, that was true of plan revisions 1-3 and was **reversed in rev 4** (module lives in `apps/long_term_forecasting/`, no packaging work, validation is not a predicate consumer). Likewise any earlier "false FAIL" framing of LTF-007 is superseded by "genuine detected execution failure". Earlier same day (third pass) — A verification pass over the fold-in below caught an **overcorrection in it**: the C1 table had claimed five tests contradict INFRA-020's acceptance criteria and must all be renegotiated. Only one must change (the `len(results) == 13` Tier-1 count); the other four are compatible and instead constrain the fix's shape — notably the stuck-flag test uses **finite** values with `flag=1`, which is not the all-NaN case at all, so an all-NaN FAIL check belongs beside it rather than replacing it. Also corrected in this pass: INFRA-020's C4 no longer contradicts its own acceptance criterion (a `detail`-string disclaimer is not sufficient); C5 is now marked **OWNER DECISION** because the station × target coverage universe is undefined and `READ_LIMIT=5000` truncation would read as a partial write; "a check registry is required" softened to one of several mechanisms; INFRA-021's "only thing suppressing false FAILs" and "all three routes crash identically" scoped to API-ready runs that reach long-term Tier 1; its exception taxonomy completed (the seasonal helper raises plain `ValueError` at `validate_pipeline.py:596-597`, not `UnsupportedLongTermModeError`, and `FileNotFoundError` needed a verdict); and the multi-date standalone simulation flagged as underspecified. **INFRA-022's draft is reconciled with its row** — it still read "Blocked behind INFRA-021" while the row called it a prerequisite, a circular dependency; observability runs one way, delivery the other, and they land as one change. A new **OPEN DECISION** is recorded in INFRA-022: its drafted mechanism (`long_term_horizon_resolver`) exposes only mode/lead/issue-day and cannot model the scheduler's tolerance window or per-model `forecast_months` (`lt_schedule_query.py:50-52`, `:88-131`), so gating built on it alone would still false-FAIL a mode that is simply unscheduled this month. Earlier same day — Folded two independent out-of-loop `codex exec` reviews into INFRA-020 and INFRA-021. Both passes confirmed the **diagnoses** and both found the **proposed fixes not implementable as written**. INFRA-020 gains constraints C1-C6: five currently-passing tests contradict its acceptance criteria (stuck-flag WARN vs demanded FAIL, the `len(results) == 13` Tier-1 count, the postprocessing tagging of the six model checks, and the two API-absent/disabled exit-0 tests, `test_validate_pipeline.py:129-140`), the zero-match guard must key on *registered* checks rather than an empty result list, and two owner decisions are open (an ML execution-expectation model, since `ML_MODE`/org skips make a calendar gate false-FAIL demo/uzhm and default-PENTAD runs; and mode provenance, since day rows carry no PENTAD/DECAD source so `BOTH` dates cross-satisfy). INFRA-021's **scope grew**: the crash is currently the only thing suppressing a stream of false FAILs, so it must ship atomically with INFRA-022 + forecast-date propagation — accordingly **INFRA-022 Medium → High** as a hard prerequisite. Also corrected in both drafts: drifted line citations (config maps `:108`/`:116`, filter `:1421-1422`, tier guards `:1449`/`:1476`, exit decision `:1284`, ML day assignment `:788`), the `run_tests.sh` command (needs `cd apps`), INFRA-021's overstated "exits 1 on every run" and its self-contradictory "raises before any long-term check runs", and the stale **INFRA-023 → INFRA-025** package-shadowing reference in INFRA-021 and INFRA-026. Field observations in INFRA-021 are now labelled external evidence — both verifiers marked them unverifiable from the repo. Earlier — 2026-08-18 — Docs-only index correction: **PP-045** row corrected (Option B merged as PR #425, not an open branch; the operational path is not the sole writer of per-model period rows; the 2026-08-18 DB probe falsified the missing-rows premise; kyg recorded as satisfied in part + owner waiver) **PP-060** filed (recalc/backfill write divergence, option (a) decided), and **PP-046** corrected from "latent" to two existing multi-year callers. Previous entry: MIG-002 (runbook §10 rollback dump-filename glob, data-loss class) Draft→**Complete**, file moved to `issues/archive/`. It was never open — commit `a1e0dfb6` (PR #359, merged 2026-06-10) both fixed the runbook **and** created the tracker row, so the row was born stale and has read `High / Draft` for ten weeks. All five acceptance criteria re-verified against trunk before closing (globs, `-n`/`-s`/`-r` guards on both `DROP DATABASE` blocks, empty gate grep, PR reference); the end-to-end backup→reset→restore rehearsal is evidenced by the merge commit's validation record, not re-run. The draft's "Required gate" (exercise §10 end-to-end before any future P7-class merge) is a standing gate for future work, not an open item. Process note: a fix-and-file-in-one-commit lands the row in `Draft` by construction — when a commit both fixes and files, the row should be created as `Complete`. Earlier 2026-08-10 — Docs-only issue-tracker cleanup: closed PREPQ-009 (long-horizon hydrograph norm decouple; fixed + merged PR #409 `c894edcd`) and FD-018 (m0 bulletin per-site target month; fixed + merged PR #420 `16fb9a9b`), both moved to `issues/archive/`. Archived the LTF monthly-horizon-value-semantics umbrella diagnosis file too — it never carried its own tracker row, so it's added here as a Complete row with no issue id (Defects A/F/J + bulletin year-rollover shipped via PR #414 `7c7c26f4` + PR #416 `d9ffa10b`; Defect G is the now-Complete FD-018). Added PP-050 (provisional id — `Bulletin` has no per-site target-period column, routed to the `sapphire/services/postprocessing` owner) — the issue file already existed in `issues/` but had never been indexed here. Earlier same day — PREPQ-011/012 (runoff pagination null-clobber, period + daily) Review→Complete: fixed + merged `fb3e4bd3` (PR #424), out-of-loop review 2026-07-23 confirmed correct + test-pinned; both moved to `issues/archive/`. Added PREPQ-013 (Low, follow-up test-coverage/defensive hardening on the same helper — not a reopened bug; `page_size=10000` exceeds every real station's archive). Earlier 2026-07-24 — Added FD-023 (deployment-level `.env` override forcing forecast bounds to `forecast ± δ`; owner-requested. Investigation found **four** distinct bound-derivation paths that disagree today, one with no δ fallback, and that overriding long-term bounds also moves published **volume** bounds; δ is `0.674×std` climatological tolerance, not a calibrated predictive interval, so its published meaning needs domain sign-off). Same session — documented `SAPPHIRE_SKILL_LEAD_AWARE` in `doc/dev/update_dev_deployment.md` (a shipped-but-undocumented default-OFF flag had made the Tajik monthly target month look one month late, and the fix look missing) and corrected the prod checklist, which required only `operational_issue_day` where the runbook requires that **and** `operational_month_lead_time`. Earlier 2026-07-23 — Added INFRA-020, INFRA-021, INFRA-022, LR-010 and PP-049 from a local pipeline health review (taj, `maxat_sapphire_2` @ `16fb9a9b`), run against a plan adversarially reviewed by out-of-loop `codex exec` (v1: not fit to execute; v3: fit). INFRA-020 (ML module validation matches zero checks and reports PASS on no evidence) independently confirmed by codex. INFRA-021 (long-term validation crashes because `validate_pipeline.py` never loads the `.env`; `quarter_horizon_value()` is called unconditionally and is NOT flag-gated) proven by controlled experiment. INFRA-022 is the false-FAIL counterpart, observable only once INFRA-021 is worked around. A provisional ML-018 (TFT-only all-NaN) was **withdrawn** after out-of-loop review showed ML-015 § Field evidence already documents that exact split and its covariate-window mechanism; the 2026-07-23 tjhm observation was merged into ML-015 as a recurrence instead. LR-010 (a legitimate "no data for forecast year" skip is indistinguishable from a genuine API write failure, so it logs "API database is now behind CSV" and exits 1) **replaces the archived tracking entry for LR-005 Issue B** — same guard, newly-recognised downstream effect, not a distinct root cause; priority left conditional on whether a legitimate skip should abort the run. PP-049 is filed as an **investigation blocked on ML-007**, not a confirmed defect: identical recalc invocations produce different month/quarter skill, but the recalc both reads unordered paginated input and mutates the store it reads, so "identical inputs" does not hold — an earlier draft claiming a confirmed idempotency defect was rewritten after out-of-loop review. ML-007 gained a 2026-07-23 reconfirmation (two no-write reads of `/long-forecast/` differed by ~18k rows; 0/6 postprocessing paginated readers order vs 4/4 in preprocessing). Also fixed a pre-existing defect in `doc/dev/review_checklist_local_template.md` §6, which directed reviewers to verify operational long-term via `long-term` / `long_term_forecasting` — both simulate paths (`simulate_forecasts.py --years 2024`), a false-PASS trap; correct target is `long-term-operational`. Earlier: 2026-07-15 — Added PREPQ-012 (daily runoff write null-clobber, no read-merge — sibling of PREPQ-011, found by 2nd adversarial review) and corrected PREPQ-011 (scoped to the period path; exclude_unset/COALESCE class-fix framing fixed — COALESCE is unsafe vs tombstones). PP-043 (DECADE skill starved on tjhm) RESOLVED→archived: not a code defect (sparse tjhm ML archive from an interrupted backfill), proven end-to-end locally; server residual re-homed to the Tajik backfill plan. Same session — Added PREPQ-011 (runoff read-merge-write clobbers non-null discharge/predictor past row 100 — silent data loss, blocks the runoff backfill), PP-041 (long_forecasts stale-aggregate invalidation, deferred P1b of PR #411), PP-042 (ensemble-exclusion display-form/DB-form mismatch — monthly latent, quarter/season exposed) and FD-021 (month_0 annotated with lead-1 skill on the SAPPHIRE_SKILL_LEAD_AWARE-OFF default path). Merge of `fix_pp_informational_metrics` — Added PP-044 (`fdc_flv` sign unit-dependence; renumbered from provisional PP-040 to avoid collision with ARIMA ModelType). Earlier 2026-07-14 — Added FD-020 (two dashboard robustness bugs: stale cross-horizon metadata reaching the forecast warning pane after a failed refresh, and a fail-loud flag parser unconditionally read on the latent quarter data path; both pre-existing on trunk, found during `develop_ltf_monthly_horizon_value` adversarial review), FD-019 (bulletin `forecast_year` derived from issue-time arithmetic instead of the target period; quarter never rolls the year, flag-OFF legacy path rolls wrong for Dec-31 lead-0), and FD-018 (m0 bulletin card hydrates from the main panel's target month instead of its own; split from `develop_ltf_monthly_horizon_value` Defect G). Earlier: 2026-06-19 — Added ML-016 (standalone run_locally.sh machine_learning crashes on empty SAPPHIRE_PREDICTION_MODE) + ML-017 (single missing ERA5 day → NaN cascade across all short-term ML; prepg interior-gap + ML covariate guard; found in local review tjhm+kghm). Earlier: 2026-06-16 — Added PP-037 (maintenance `model_short` KeyError on empty DECAD read, Ready — plan through two review cycles; Phase 1 is the minimum-safe production fix). 2026-04-16 — SEC-006 Draft→Review (PR #330); P-005/P-006 Complete; LTF-005 Draft→Review; added P-004, FD-014, PP-028b.
Last updated: 2026-08-18 — Findings from the kyg (kghm) end-to-end local review, folded in: **INFRA-029** filed (`setup_library`'s import-time root-logger config caps 13 entry points at WARNING — proven by direct execution, and the reason PP-045's CLI emits nothing), **INFRA-030** (skipped modules leave no summary line), **ML-019** (`recalculate_nan_forecasts` claims replacement unconditionally), **ML-020** (an all-null ML issue date is unreachable by either maintenance path — investigation). Updated with second/third reproductions or corrections: **PP-040** (the "no env enables ARIMA" survey missed a second config repo), **PREPQ-014** (kyg reproduction at the same cardinality plus owner input that virtual stations legitimately have no norms), **INFRA-026** (kyg snow reproduction with a live backfill boundary), **PP-045** (§D: its own CLI's output is suppressed; runbook corrected).

Previous entry: 2026-08-18 (INFRA-022 decision) — Owner chose **option (d)** for the long-term schedule authority: the mode-activity decision gets exactly one definition. Planned in `doc/plans/working/lt_schedule_authority_extraction_plan.md`, now at **rev 4** after five review passes. **Placement, as settled in rev 4**: `apps/long_term_forecasting/lt_schedule_rules.py` — *not* `iEasyHydroForecast`, because validation consumes INFRA-028's manifest rather than the predicates (it already gets horizon values from `long_term_horizon_resolver`). That deleted a whole phase — the `nearest_scheduled_issue_date` move, which had been vestigial since rev 3 put the call back in the caller — plus the wheel-include-list, namespace-mapping, shim and Docker-packaging work that existed only to cross the package boundary. The extraction is **independent cleanup, not a prerequisite** of INFRA-022; its real second consumer is LTF-007. Its placement remains conditional on INFRA-028's open missing-manifest decision, which now gates P1 in the plan's phase graph. Two issues filed out of these reviews: **INFRA-028** (long-term runs emit no record of what they were supposed to produce — the plan's Phase 4 was a placeholder, and a *re-derived* schedule cannot see manual overrides, execution outcome, or a late forecast's snapped-back date; a prerequisite of INFRA-022) and **LTF-007** (the scheduler admits modes at 10 days from issue day while execution refuses at more than 5, so an admitted mode can produce nothing — conditional on both scheduler gates passing and every model's real offset exceeding 5; **semantics: that is a genuine detected execution failure, not a false FAIL**). A **third** live schedule authority was found in the last pass: the local runner's fallback (`run_locally.sh:232-261`) reimplements the wrap distance and its own five-day window, and is used precisely when the schedule query fails. The review also corrected the plan itself: a mode-level predicate cannot preserve the non-operational short-circuit (which touches no config at all) or the lazy per-model `break`, so the extracted API is a set of caller-driven predicates rather than one function owning the loop; `ForecastConfig` is not merely a loader but **rewrites every model's `general_config.json`** during load; the new module must be added to `iEasyHydroForecast/pyproject.toml`'s explicit wheel include list; `day_distance` must stay importable from `lt_schedule_query`; and the "single-source the tolerance" claim holds only between scheduler and validator — `bin/utils/migration_py/long_forecast.py` keeps its own `_ALWAYS_SKIP_MODES`. **Superseded entries follow — read them as history, not as current guidance.** Where an earlier entry describes the extraction as landing in `iEasyHydroForecast`, requiring a wheel-include-list update, or sharing a tolerance with the validator, that was true of plan revisions 1-3 and was **reversed in rev 4** (module lives in `apps/long_term_forecasting/`, no packaging work, validation is not a predicate consumer). Likewise any earlier "false FAIL" framing of LTF-007 is superseded by "genuine detected execution failure".

Earlier same day (third pass) — A verification pass over the fold-in below caught an **overcorrection in it**: the C1 table had claimed five tests contradict INFRA-020's acceptance criteria and must all be renegotiated. Only one must change (the `len(results) == 13` Tier-1 count); the other four are compatible and instead constrain the fix's shape — notably the stuck-flag test uses **finite** values with `flag=1`, which is not the all-NaN case at all, so an all-NaN FAIL check belongs beside it rather than replacing it. Also corrected in this pass: INFRA-020's C4 no longer contradicts its own acceptance criterion (a `detail`-string disclaimer is not sufficient); C5 is now marked **OWNER DECISION** because the station × target coverage universe is undefined and `READ_LIMIT=5000` truncation would read as a partial write; "a check registry is required" softened to one of several mechanisms; INFRA-021's "only thing suppressing false FAILs" and "all three routes crash identically" scoped to API-ready runs that reach long-term Tier 1; its exception taxonomy completed (the seasonal helper raises plain `ValueError` at `validate_pipeline.py:596-597`, not `UnsupportedLongTermModeError`, and `FileNotFoundError` needed a verdict); and the multi-date standalone simulation flagged as underspecified. **INFRA-022's draft is reconciled with its row** — it still read "Blocked behind INFRA-021" while the row called it a prerequisite, a circular dependency; observability runs one way, delivery the other, and they land as one change. A new **OPEN DECISION** is recorded in INFRA-022: its drafted mechanism (`long_term_horizon_resolver`) exposes only mode/lead/issue-day and cannot model the scheduler's tolerance window or per-model `forecast_months` (`lt_schedule_query.py:50-52`, `:88-131`), so gating built on it alone would still false-FAIL a mode that is simply unscheduled this month.

Earlier same day — Folded two independent out-of-loop `codex exec` reviews into INFRA-020 and INFRA-021. Both passes confirmed the **diagnoses** and both found the **proposed fixes not implementable as written**. INFRA-020 gains constraints C1-C6: five currently-passing tests contradict its acceptance criteria (stuck-flag WARN vs demanded FAIL, the `len(results) == 13` Tier-1 count, the postprocessing tagging of the six model checks, and the two API-absent/disabled exit-0 tests, `test_validate_pipeline.py:129-140`), the zero-match guard must key on *registered* checks rather than an empty result list, and two owner decisions are open (an ML execution-expectation model, since `ML_MODE`/org skips make a calendar gate false-FAIL demo/uzhm and default-PENTAD runs; and mode provenance, since day rows carry no PENTAD/DECAD source so `BOTH` dates cross-satisfy). INFRA-021's **scope grew**: the crash is currently the only thing suppressing a stream of false FAILs, so it must ship atomically with INFRA-022 + forecast-date propagation — accordingly **INFRA-022 Medium → High** as a hard prerequisite. Also corrected in both drafts: drifted line citations (config maps `:108`/`:116`, filter `:1421-1422`, tier guards `:1449`/`:1476`, exit decision `:1284`, ML day assignment `:788`), the `run_tests.sh` command (needs `cd apps`), INFRA-021's overstated "exits 1 on every run" and its self-contradictory "raises before any long-term check runs", and the stale **INFRA-023 → INFRA-025** package-shadowing reference in INFRA-021 and INFRA-026. Field observations in INFRA-021 are now labelled external evidence — both verifiers marked them unverifiable from the repo.

Earlier — 2026-08-18 — Docs-only index correction: **PP-045** row corrected (Option B merged as PR #425, not an open branch; the operational path is not the sole writer of per-model period rows; the 2026-08-18 DB probe falsified the missing-rows premise; kyg recorded as satisfied in part + owner waiver) **PP-060** filed (recalc/backfill write divergence, option (a) decided), and **PP-046** corrected from "latent" to two existing multi-year callers.

Previous entry: MIG-002 (runbook §10 rollback dump-filename glob, data-loss class) Draft→**Complete**, file moved to `issues/archive/`. It was never open — commit `a1e0dfb6` (PR #359, merged 2026-06-10) both fixed the runbook **and** created the tracker row, so the row was born stale and has read `High / Draft` for ten weeks. All five acceptance criteria re-verified against trunk before closing (globs, `-n`/`-s`/`-r` guards on both `DROP DATABASE` blocks, empty gate grep, PR reference); the end-to-end backup→reset→restore rehearsal is evidenced by the merge commit's validation record, not re-run. The draft's "Required gate" (exercise §10 end-to-end before any future P7-class merge) is a standing gate for future work, not an open item. Process note: a fix-and-file-in-one-commit lands the row in `Draft` by construction — when a commit both fixes and files, the row should be created as `Complete`.

Earlier 2026-08-10 — Docs-only issue-tracker cleanup: closed PREPQ-009 (long-horizon hydrograph norm decouple; fixed + merged PR #409 `c894edcd`) and FD-018 (m0 bulletin per-site target month; fixed + merged PR #420 `16fb9a9b`), both moved to `issues/archive/`. Archived the LTF monthly-horizon-value-semantics umbrella diagnosis file too — it never carried its own tracker row, so it's added here as a Complete row with no issue id (Defects A/F/J + bulletin year-rollover shipped via PR #414 `7c7c26f4` + PR #416 `d9ffa10b`; Defect G is the now-Complete FD-018). Added PP-050 (provisional id — `Bulletin` has no per-site target-period column, routed to the `sapphire/services/postprocessing` owner) — the issue file already existed in `issues/` but had never been indexed here.

Earlier same day — PREPQ-011/012 (runoff pagination null-clobber, period + daily) Review→Complete: fixed + merged `fb3e4bd3` (PR #424), out-of-loop review 2026-07-23 confirmed correct + test-pinned; both moved to `issues/archive/`. Added PREPQ-013 (Low, follow-up test-coverage/defensive hardening on the same helper — not a reopened bug; `page_size=10000` exceeds every real station's archive).

Earlier 2026-07-24 — Added FD-023 (deployment-level `.env` override forcing forecast bounds to `forecast ± δ`; owner-requested. Investigation found **four** distinct bound-derivation paths that disagree today, one with no δ fallback, and that overriding long-term bounds also moves published **volume** bounds; δ is `0.674×std` climatological tolerance, not a calibrated predictive interval, so its published meaning needs domain sign-off). Same session — documented `SAPPHIRE_SKILL_LEAD_AWARE` in `doc/dev/update_dev_deployment.md` (a shipped-but-undocumented default-OFF flag had made the Tajik monthly target month look one month late, and the fix look missing) and corrected the prod checklist, which required only `operational_issue_day` where the runbook requires that **and** `operational_month_lead_time`.

Earlier 2026-07-23 — Added INFRA-020, INFRA-021, INFRA-022, LR-010 and PP-049 from a local pipeline health review (taj, `maxat_sapphire_2` @ `16fb9a9b`), run against a plan adversarially reviewed by out-of-loop `codex exec` (v1: not fit to execute; v3: fit). INFRA-020 (ML module validation matches zero checks and reports PASS on no evidence) independently confirmed by codex. INFRA-021 (long-term validation crashes because `validate_pipeline.py` never loads the `.env`; `quarter_horizon_value()` is called unconditionally and is NOT flag-gated) proven by controlled experiment. INFRA-022 is the false-FAIL counterpart, observable only once INFRA-021 is worked around. A provisional ML-018 (TFT-only all-NaN) was **withdrawn** after out-of-loop review showed ML-015 § Field evidence already documents that exact split and its covariate-window mechanism; the 2026-07-23 tjhm observation was merged into ML-015 as a recurrence instead. LR-010 (a legitimate "no data for forecast year" skip is indistinguishable from a genuine API write failure, so it logs "API database is now behind CSV" and exits 1) **replaces the archived tracking entry for LR-005 Issue B** — same guard, newly-recognised downstream effect, not a distinct root cause; priority left conditional on whether a legitimate skip should abort the run. PP-049 is filed as an **investigation blocked on ML-007**, not a confirmed defect: identical recalc invocations produce different month/quarter skill, but the recalc both reads unordered paginated input and mutates the store it reads, so "identical inputs" does not hold — an earlier draft claiming a confirmed idempotency defect was rewritten after out-of-loop review. ML-007 gained a 2026-07-23 reconfirmation (two no-write reads of `/long-forecast/` differed by ~18k rows; 0/6 postprocessing paginated readers order vs 4/4 in preprocessing). Also fixed a pre-existing defect in `doc/dev/review_checklist_local_template.md` §6, which directed reviewers to verify operational long-term via `long-term` / `long_term_forecasting` — both simulate paths (`simulate_forecasts.py --years 2024`), a false-PASS trap; correct target is `long-term-operational`.

Earlier: 2026-07-15 — Added PREPQ-012 (daily runoff write null-clobber, no read-merge — sibling of PREPQ-011, found by 2nd adversarial review) and corrected PREPQ-011 (scoped to the period path; exclude_unset/COALESCE class-fix framing fixed — COALESCE is unsafe vs tombstones). PP-043 (DECADE skill starved on tjhm) RESOLVED→archived: not a code defect (sparse tjhm ML archive from an interrupted backfill), proven end-to-end locally; server residual re-homed to the Tajik backfill plan. Same session — Added PREPQ-011 (runoff read-merge-write clobbers non-null discharge/predictor past row 100 — silent data loss, blocks the runoff backfill), PP-041 (long_forecasts stale-aggregate invalidation, deferred P1b of PR #411), PP-042 (ensemble-exclusion display-form/DB-form mismatch — monthly latent, quarter/season exposed) and FD-021 (month_0 annotated with lead-1 skill on the SAPPHIRE_SKILL_LEAD_AWARE-OFF default path). Merge of `fix_pp_informational_metrics` — Added PP-044 (`fdc_flv` sign unit-dependence; renumbered from provisional PP-040 to avoid collision with ARIMA ModelType).

Earlier 2026-07-14 — Added FD-020 (two dashboard robustness bugs: stale cross-horizon metadata reaching the forecast warning pane after a failed refresh, and a fail-loud flag parser unconditionally read on the latent quarter data path; both pre-existing on trunk, found during `develop_ltf_monthly_horizon_value` adversarial review), FD-019 (bulletin `forecast_year` derived from issue-time arithmetic instead of the target period; quarter never rolls the year, flag-OFF legacy path rolls wrong for Dec-31 lead-0), and FD-018 (m0 bulletin card hydrates from the main panel's target month instead of its own; split from `develop_ltf_monthly_horizon_value` Defect G).

Earlier: 2026-06-19 — Added ML-016 (standalone run_locally.sh machine_learning crashes on empty SAPPHIRE_PREDICTION_MODE) + ML-017 (single missing ERA5 day → NaN cascade across all short-term ML; prepg interior-gap + ML covariate guard; found in local review tjhm+kghm).

Earlier: 2026-06-16 — Added PP-037 (maintenance `model_short` KeyError on empty DECAD read, Ready — plan through two review cycles; Phase 1 is the minimum-safe production fix).

2026-04-16 — SEC-006 Draft→Review (PR #330); P-005/P-006 Complete; LTF-005 Draft→Review; added P-004, FD-014, PP-028b.
